Start
Initialize array of months (12 elements)
Initialize array of temperatures (12 elements)
Prompt user to enter average temperature for each month
Store each temperature in the temperature array

Prompt user to enter a month name or "year"
IF input is a valid month
    Find index of month
    Display month and its temperature
ELSE IF input is "year"
    Display all months and their temperatures
    Calculate yearly average
    Find highest and lowest temperatures
    Display yearly average, highest, and lowest
ELSE
    Display error message

End
